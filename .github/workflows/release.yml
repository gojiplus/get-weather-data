name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  release:
    uses: gojiplus/py-canon/.github/workflows/reusable-release.yml@v1
    permissions:
      contents: write
      id-token: write
